# 프로젝트 소개 (발표용 요약)

## 1. 기존 배포본 리마인드 (aruco_trigger_capture.py까지)
- **기본 흐름**  
  1) `roslaunch mm_cam usb_cam.launch` – USB 카메라 노드  
  2) `roslaunch omo_r1mini_bringup omo_r1mini_bringup.launch` – OMO R1 mini 구동  
  3) `python lane_follower.py` – 차선 인식 + `/cmd_vel` 제어  
  4) `aruco_trigger_capture.py` – ArUco 기반 회전·캡처 매크로  
- **핵심 개념**  
- `/usb_cam` 영상으로 차선을 따라가면서, 동일 프레임을 ArUcoDetector에 넘겨 ID를 감지  
  - ID별 rule에 따라 회전하거나 이미지를 저장하고 다시 차선 모드로 복귀  
  - 단순 캡처 및 회전 중심, YOLO 연동 없음

> **포인트:** 학생들이 이미 실습했던 버전이라는 점을 간단히 짚고, 새 버전으로 무엇이 확장되는지 대비할 수 있도록 리마인드.

---

## 2. 업그레이드: aruco_trigger_capture_yolo.py
- **추가 기능**
  - **YOLO 캡처 / 별도 이미지 저장**: `yolo_capture` 액션이 생겨 특정 ID에서 YOLO용 데이터를 자동 수집  
  - (근데 못 씀)**QR 코드 로깅**: pyzbar로 QR 텍스트를 감지해 ROS 로그에 출력  
  - **Per-ID 쿨다운 & 연속 프레임 필터**: 노이즈 감지를 줄이고 안정적으로 트리거
  - **pending_actions** 큐를 통해 회전/캡처/YOLO 캡처를 순차 실행
- **폴더 구조**
  - `~/catkin_ws/src/ROKAF_Autonomous_Car_2025/images` – 기존 화재/미션 캡처  
  - `~/catkin_ws/src/ROKAF_Autonomous_Car_2025/yolo_images` – YOLO 캡처 전용

---

## 3. lane_follower-PID.py + yolo_image.py = 통합 파이프라인
### (1) `lane_follower-PID.py`
- BEV 변환 + 슬라이딩 윈도 기반 차선 추종 → PID 제어로 `/cmd_vel` 생성
- 같은 콜백에서 `ArucoTriggerCaptureYOLO`를 구동 → 마커 감지 시 회전/캡처/YOLO 캡처 자동 실행

### (2) `yolo_image.py`
- `yolo_images` 디렉터리 감시 → 새 파일 발견 시 Ultralytics YOLO(`config/best_rokaf.pt`) 추론
- 이미지별/누적 클래스 카운트를 콘솔에 출력 → 현장에서 데이터 품질 즉시 확인

### (3) 실행 순서 (발표용)
1. `roslaunch mm_cam usb_cam.launch`  
2. `roslaunch omo_r1mini_bringup omo_r1mini_bringup.launch`  
3. `python lane_follower-PID.py`  ← 차선+Trigger  
4. `python yolo_image.py`         ← 캡처 모니터링  
→ **결과:** 주행 중 특정 마커를 만나면 회전·이미지 캡처·YOLO 캡처가 즉시 수행되고, 감시 스크립트가 곧바로 인식 결과를 보여줌.

---

## 4. 발표 키 포인트
| 구분 | 기존 배포본 | 업그레이드 버전 |
| --- | --- | --- |
| 트리거 | 단순 회전/캡처 | 회전 + 일반 캡처 + YOLO 캡처 + QR 로깅 |
| 제어 | 기본 차선 추종 | PID 기반 차선 제어 (heading/lateral 가중치) |
| 데이터 플로우 | 수동 확인 | 캡처→폴더→YOLO 추론 로그 자동화 |
| 확장성 | rule 고정 | `self.rules`, `IMAGE_DIR` 수정으로 시나리오 변경 용이 |

---

## 5. 발표 시 권장 구성
1. **리마인드** – 학생들이 사용했던 `aruco_trigger_capture.py` 흐름을 1~2장 슬라이드로 요약  
2. **신규 기능 소개** – YOLO 캡처, QR, PID 개선 등을 bullet로 정리  
3. **통합 데모 플로우** – 두 스크립트를 동시에 실행했을 때의 데이터 흐름을 다이어그램으로 제시  
4. **로그/결과 예시** – `yolo_image.py` 출력 스크린샷 또는 텍스트 예시  
5. **활용 아이디어** – 마커 rule 변경, 추가 데이터 레이블링 등 확장 방안
